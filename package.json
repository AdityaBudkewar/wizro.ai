{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/**/*.{js,jsx,ts,tsx}": [
        "npm run lint --prefix frontend --"
    ],
    "backend/**/*.{js,jsx,ts}": [
      "npm run lint --prefix backend --"
    ]
  }
}
